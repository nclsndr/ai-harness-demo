# Cursor

- **Skills**: `.cursor/skills/<name>/SKILL.md`
- **Rules** (optional): `.cursor/rules/*.mdc`
- **Hooks** (optional): `.cursor/hooks.json`

This repo includes `conventional-commit` for semantic git commits.
